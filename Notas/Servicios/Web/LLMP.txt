# Requiere
- Apache
- MariaDB

# Instalamos el soporte de PHP para lighttpd y MariaDB
apt -yq install php php-cgi php-mysql

# Habilitamos el modulo de PHP para lighttpd
lighttpd-enable-mod fastcgi-php

# Verificamos la configuración de Lighttpd
lighttpd -t -f /etc/lighttpd/lighttpd.conf

# Reiniciamos el servicio
systemctl restart lighttpd
