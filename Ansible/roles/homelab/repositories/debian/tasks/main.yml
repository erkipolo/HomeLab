- name: Add repositories URL to hosts
  tags: HomeLab
  ansible.builtin.lineinfile:
    dest: /etc/hosts
    regexp: '^192\.168\.56\.1 deb'
    line: "192.168.56.1 deb.debian.org security.debian.org"
