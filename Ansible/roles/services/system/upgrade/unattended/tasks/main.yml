- name: Install Unattended Upgrades
  tags: UnattendedUpgrades
  ansible.builtin.apt:
    name:
      - unattended-upgrades
    state: latest

- name: Setting what and when to unattended upgrades
  tags: UnattendedUpgrades
  ansible.builtin.copy:
    dest: /etc/apt/apt.conf.d/52unattended-upgrades-local
    owner: root
    group: root
    mode: '0644'
    content: |
      Unattended-Upgrade::Origins-Pattern {
        "origin=Debian,codename=${distro_codename}-updates";
        "origin=Debian,codename=${distro_codename},label=Debian";
        "origin=Debian,codename=${distro_codename},label=Debian-Security";
        "origin=Debian,codename=${distro_codename}-security,label=Debian-Security";
      };
      Unattended-Upgrade::Package-Blacklist {
      };
      Unattended-Upgrade::AutoFixInterruptedDpkg "true";
      Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
      Unattended-Upgrade::Remove-New-Unused-Dependencies "true";
      Unattended-Upgrade::Remove-Unused-Dependencies "true";
      Unattended-Upgrade::Automatic-Reboot "true";
      Unattended-Upgrade::Automatic-Reboot-WithUsers "true";
      Unattended-Upgrade::Automatic-Reboot-Time "04:00";
  notify: restart_unattended

- name: Config unattended upgrades log
  tags: UnattendedUpgrades
  ansible.builtin.copy:
    dest: /etc/logrotate.d/unattended-upgrades
    owner: root
    group: root
    mode: '0644'
    content: |
      /var/log/unattended-upgrades/unattended-upgrades.log 
      /var/log/unattended-upgrades/unattended-upgrades-dpkg.log
      /var/log/unattended-upgrades/unattended-upgrades-shutdown.log
      {
        daily
        dateext
        dateformat -%Y%m%d-%H%M%S
        missingok
        rotate 365
        compress
      }

- name: Enable unattended update, download, upgrade and clean Packages
  tags: UnattendedUpgrades
  ansible.builtin.copy:
    dest: /etc/apt/apt.conf.d/20auto-upgrades
    owner: root
    group: root
    mode: '0644'
    content: |
      APT::Periodic::Update-Package-Lists "1";
      APT::Periodic::Unattended-Upgrade "1";
      APT::Periodic::AutocleanInterval "7";
      APT::Periodic::Download-Upgradeable-Packages "1";
  notify: restart_unattended

